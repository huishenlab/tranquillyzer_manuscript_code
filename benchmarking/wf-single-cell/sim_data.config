env {
    NUMBA_CACHE_DIR='sim_data_work'
    MPLCONFIGDIR='sim_data_work'
}

process {
    withName:call_adapter_scan {
        memory = "12 GB"
    }
}

process {
    withName:cluster_umis {
        memory = "12 GB"
    }
}
